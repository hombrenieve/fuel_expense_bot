# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram: https://t.me/botfather
TELEGRAM_TOKEN=your_bot_token_here

# Database Configuration
# Host where your MariaDB/MySQL database is running
DB_HOST=localhost

# Database port (default MySQL/MariaDB port is 3306)
DB_PORT=3306

# Database username with access to the fuel expense database
DB_USERNAME=fuel_bot

# Database password for the user
DB_PASSWORD=your_password_here

# Name of the database (must be created before running the bot)
DB_DATABASE=fuel_expense_bot

# Maximum number of concurrent database connections (5-20 recommended)
DB_MAX_CONNECTIONS=5

# Bot Configuration
# Default monthly spending limit for new users (in euros)
DEFAULT_LIMIT=210.00

# Logging Configuration (optional)
# Levels: error, warn, info, debug, trace
# Format: module=level or just level for all modules
# Examples:
#   RUST_LOG=info                           # Info level for everything
#   RUST_LOG=telegram_fuel_bot=debug        # Debug for bot, default for others
#   RUST_LOG=telegram_fuel_bot=debug,info   # Debug for bot, info for dependencies
RUST_LOG=telegram_fuel_bot=info

